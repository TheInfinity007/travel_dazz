import React from 'react';
import {
    CircularProgress,
    Grid,
    Typography,
    InputLabel,
    MenuItem,
    FormControl,
    Select,
} from '@mui/material';

const List = () => {
    return <h1>List</h1>;
};

export default List;
